1、CRF++自带可执行文件，无需编译
2、GibbsLDA++没有自带可执行文件，需要手动编译。源代码中有几处地方需要根据实际情况修改。
其中析构函数有问题，程序退出时数组访问越界，为方便起见在此屏蔽了析构函数，后果是可能会造成内存泄漏。
3、压缩包中已经包含了可执行文件的Release版本、执行中用到的动态链接库以及数据集、图片资源等。
如果重新编译Debug版本，需要把这些资源文件复制到Debug目录下。
4、编译前需要先还原Nuget包